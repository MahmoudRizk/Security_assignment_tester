#include <iostream>
#include <string>

using namespace std;

int main(int argc, char*argv[]){

        string p =argv[1];
	string q =argv[2];
	string e =argv[3];

	int x = stoi(p);
	int y = stoi(q);
	int z = stoi(e);
  
  	int result = p + q ;
 	 cout << result << endl;
  return 0;
}
